# migration-cli

Migration CLI: schema/version management and data maintenance commands.

允许依赖：application, storage-postgres, message-local, foundation。
禁止：HTTP/Axum runtime, direct SQL queries, global mutable state。

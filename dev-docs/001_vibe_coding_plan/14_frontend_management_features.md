# 14. 管理核心页面

### WEB-004：组织与空间管理
**前置：** ORG-003、WEB-002。
- [x] 树懒加载、搜索、创建、移动和删除；移动显示影响范围并处理 ETag 冲突。
- [x] 组织/空间使用不同图标、类型和 API，禁止混淆 ID。
**测试：** 深树、空状态、并发 move、403、失败回滚 UI。

### WEB-005：用户、角色和范围
**前置：** IAM-004、AUTH-003、WEB-003。
- [x] 用户生命周期、密码/MFA 管理、服务账号 key 一次性展示。
- [x] 角色权限矩阵和 binding scope 编辑器；范围预览调用服务端鉴权解释接口。
**测试：** 表单边界、过期 binding、key 不再次展示、权限被撤销。

### WEB-006：设备、摄像机和绑定
**前置：** RES-003、WEB-003。
- [x] 列表/详情/标签/组织区域/敏感级别；协议投影明确 observed/stale。
- [x] ExternalBinding 冲突要求显式处置，不允许 UI 最后写覆盖。
**测试：** 大列表游标、stale、冲突、Retired、敏感摄像机隐藏字段。

### WEB-007：审计与配置
**前置：** AUD-002、WEB-003。
- [ ] 审计只读检索、详情和安全 diff；不渲染未清理 HTML。
- [ ] 配置按 definition 生成表单；secret 只允许替换/清除，不回显。
**测试：** XSS、长内容、动态配置失败、敏感值不进 DOM snapshot。


# VID-002 视频 REST 与审计报告

## 任务目标

在应用层将 `PlaybackEntitlement` 映射为安全 DTO，包含 source 与 policy，不含设备凭据。

## 变更摘要

- `crates/application/src/media.rs`：`PlaybackEntitlementDto` 新增 `main_source`/`sub_source`/`player_policy`；`CreateEntitlementRequest` 增加 `protocol`。
- `MediaService` 分别使用 `media:entitlement:create/read/revoke` 权限，并将错误映射为 `PlatformError`。

## 验证

- `cargo clippy -p application --all-targets -- -D warnings` 通过。

## 未完成/降级

- REST 路由与 OpenAPI 定义尚未挂载；需 `VID-001` upstream media engine 后才能生成真实 source。

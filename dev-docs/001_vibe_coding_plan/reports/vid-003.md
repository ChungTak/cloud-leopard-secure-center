# VID-003 SecurityPlayer 包装报告

## 任务目标

提供安全播放器包装，处理生命周期、token 刷新、主子码流切换与诊断脱敏。

## 变更摘要

- `web/packages/player/src/SecurityPlayer.tsx`：增加 `onLoad`/`onDestroy`/`onTokenExpired`；`useEffect` cleanup 中调用 stop/destroy；`redactUrl` 对 token 参数脱敏。
- Token 不渲染、不进入 DOM 文本，仅在隐藏 input 中保存以便（可选）程序化访问。

## 验证

- `pnpm typecheck` 与 `pnpm test` 通过。

## 未完成/降级

- 真实 `@cheetah-media/web` 组件未加载；组件以 `unsupported` fallback 运行并降级到 native `<video>`。

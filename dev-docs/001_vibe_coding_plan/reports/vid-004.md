# VID-004 多画面与浏览器矩阵报告

## 任务目标

支持多画面布局、Worker/Wasm 自托管解码、CSP/SRI/COOP/COEP 与浏览器兼容降级。

## 变更摘要

- `web/packages/player/src/useSecurityPlayerWorker.ts`：`loadSecurityPlayerWorker` stub 返回 `unsupported`。
- `web/packages/player/src/playerConfig.ts`：`PlayerSecurityPolicy`、`defaultPlayerSecurityPolicy`、`securePlayerBrowserMatrix`。
- `MultiPlayerLayout` 已支持 1/4/9/16 窗格。

## 验证

- `pnpm typecheck` 与 `pnpm test` 通过。

## 未完成/降级

- 真实 Worker/Wasm/codec 由上游 media engine 提供；CSP 头由宿主/反向代理注入；Chromium/Firefox/WebKit 自动化矩阵在 Phase 1 不运行。
